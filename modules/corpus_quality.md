# 语料质量与答案强校正模块（骨架）

为每条答案维护状态：`official`（可核验官方发布）、`licensed`（已授权来源）、`public_reference`（公开但非官方）、`conflict`、`unverified`。质量排序不能由销量、点赞或模型自信度单独决定。

强校正规则：

- 答案键与题号必须完整且可追溯；
- 解析必须引用原文或题干证据；
- 多来源冲突时保留全部版本并建立争议记录；
- 未确认答案不进入高置信度训练模板；
- 答案隔离分析先于答案加载，不能让答案倒灌改写前面的证据链；
- 只有在数据和版权状态明确时才允许进入发布包。
